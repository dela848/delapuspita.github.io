// Synchronous
//menambahkan nama 
const getUserSyn = (id)=>{
    const nama = id === 1 ? 'santika' : 'galih'
} 
const userSatu = getUserSync(1);
console.log(userSatu);
 
const usreDua = getUserSync(2);
console.log(userDua);

const halo = 'Hello Wworld!';
console.log(halo);

